{% block header %}
    <section id="header">
        <div class="navbar navbar-default navbar-fixed-top" role="navigation">
            <div class="container-fluid">
                <div class="navbar-header no-padding">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" style="display: none;" href="{{ app.urlFor('home') }}">{{ app.getName() }}</a>
                </div>
                <div class="navbar-collapse collapse">
                    <div class="app-title width-full text-center">
                        <h3><a href="{{ app.urlFor('home') }}">{{ app.getName() }}</a></h3>
                    </div>
                    <form name="searchPhoto" class="navbar-form" role="form" action="{{ app.urlFor('list') }}" method="POST">
                        <div class="input-group width-full">
                            <span class="input-group-addon">#</span>
                            <input class="form-control width-full" type="text" name="hashtag" value="{{ hashtag }}" placeholder="Digite uma #hashtag e descubra lindas fotos do Instagram" />
                            <span class="input-group-btn">
                                <button type="submit" class="btn btn-primary">Go!</button>
                            </span>
                        </div>
                    </form>
                </div><!--/.navbar-collapse -->
            </div>
        </div>
    </section>
{% endblock header %}